[package]
name = "n0xis-mcp"
description = "MCP frontend — agent-native tools over the same pipeline as the CLI. Placeholder in Phase 1; built out in Phase 5 (the moat)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
n0xis-contracts = { workspace = true }
n0xis-pipeline = { workspace = true }
